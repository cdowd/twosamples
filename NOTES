#   Build and Reload Package:  'Ctrl + Shift + B'
#   Check Package:             'Ctrl + Shift + E'
#   Test Package:              'Ctrl + Shift + T'

# Release steps
1. make changes
2. check r-cmd-check passes on github/website
3. check reverse dependencies (revdepcheck::revdep_check())
  - may need to edit ~/.R/MAKEVARS to get FRESA.CAD to install.
    - FC = usr/local/opt/gcc/bin/gfortran
      F77 = /usr/local/opt/gcc/bin/gfortran
      FLIBS = -L/usr/local/opt/gcc/lib
4. update news
5. bump version number
6. rebuild docs, build site.
7. devtools::check_rhub()
8. update cran-comments.md
9. Submit (devtools::)

To do:

1. Sort out Travis integration
2. Submit


Future:
Multiple Dimensions (Sampling Routine)
Relax RCPP dependence
Clean up 'permutation_test_interpreter'
Glance at functions under 'leftovers.R'

Notes:
Switching if(x<0) x = -1*x to x=abs(x) doesn't work for all R platforms....
